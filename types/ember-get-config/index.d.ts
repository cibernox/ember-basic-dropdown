declare const config: {
  environment: string;
  modulePrefix: string;
  podModulePrefix: string;
  locationType: string;
  rootURL: string;

  'ember-basic-dropdown': {
    destination: string
  } | undefined
};

export default config;
